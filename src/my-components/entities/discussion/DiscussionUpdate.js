import React from 'react';
import PropTypes from 'prop-types';

DiscussionUpdate.propTypes = {
    
};

function DiscussionUpdate(props) {
    return (
        <div></div>
    );
}

export default DiscussionUpdate;